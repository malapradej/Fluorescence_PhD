#!/usr/bin/python
"""
A script that creates an Emulator for a given directory (sun, view, azimuth
angle configuration). Loops over all files and reads data, creates an Emulator
for that directory and saves in to disk.
Run the script with directory path as argument to script.
"""

